# Notes

Dry-run smoke training on deterministic mock trajectories only. This run is for code-path validation and must not be reported as a scientific result.

When configured with WAM-GRU, future latent targets are produced by the frozen smoke time-index encoder. No real visual backbone, SNN, or closed-loop rollout is evaluated.
