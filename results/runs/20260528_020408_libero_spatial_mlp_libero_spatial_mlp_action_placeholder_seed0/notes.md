# Notes

Dry-run smoke training on deterministic mock trajectories only. This run is for code-path validation and must not be reported as a scientific result.

The first MLP baseline is action-only: stub visual/text encoders, no future latent loss, no GRU, and no SNN.
